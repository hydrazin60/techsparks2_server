# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @tech-sparks2-server/auth_workspace`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t tech-sparks2-server-auth_workspace`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system tech-sparks2-server-auth_workspace && \
          adduser --system -G tech-sparks2-server-auth_workspace tech-sparks2-server-auth_workspace

COPY apps/auth_workspace/dist tech-sparks2-server-auth_workspace/
COPY apps/auth_workspace/package.json tech-sparks2-server-auth_workspace/
RUN chown -R tech-sparks2-server-auth_workspace:tech-sparks2-server-auth_workspace .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix tech-sparks2-server-auth_workspace --omit=dev -f install

CMD [ "node", "tech-sparks2-server-auth_workspace" ]
